import { EndingFacturationTextPipe } from './ending-facturation-text.pipe';

describe('EndingFacturationTextPipe', () => {
  it('create an instance', () => {
    const pipe = new EndingFacturationTextPipe();
    expect(pipe).toBeTruthy();
  });
});
