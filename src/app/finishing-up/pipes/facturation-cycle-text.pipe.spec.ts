import { FacturationCycleTextPipe } from './facturation-cycle-text.pipe';

describe('FacturationCycleTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FacturationCycleTextPipe();
    expect(pipe).toBeTruthy();
  });
});
